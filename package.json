{
  "name": "derivative-chromosome-utils",
  "version": "0.1.0",
  "description": "Reconstruct derivative chromosomes from VCF breakend (BND) records",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "node --experimental-strip-types --experimental-vm-modules node_modules/.bin/vitest run",
    "test:watch": "node --experimental-strip-types --experimental-vm-modules node_modules/.bin/vitest"
  },
  "license": "MIT",
  "devDependencies": {
    "canvas": "^3.2.1",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
